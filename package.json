{
  "name": "maori-app",
  "version": "1.0.0",
  "description": "Registro presenze Reparto Scout Maori",
  "scripts": {
    "dev": "vite",
    "build": "node build-config.js && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "type-check": "tsc --noEmit",
    "type-check:watch": "tsc --noEmit --watch"
  },
  "engines": {
    "node": ">=14.x"
  },
  "devDependencies": {
    "@types/node": "^20.19.27",
    "@vitest/ui": "^1.0.0",
    "jsdom": "^27.3.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.0",
    "vitest": "^1.0.0"
  },
  "dependencies": {
    "firebase": "^12.7.0"
  }
}
